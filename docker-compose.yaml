version: '3.6'

services:
  jekyll:
    image: jekyll/jekyll:latest
    #command: bundler
    command: jekyll serve
    ports:
      - 4000:4000
      - 35729:35729
    volumes:
      - .:/srv/jekyll
